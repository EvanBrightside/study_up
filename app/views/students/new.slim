.modal-header
  h5.modal-title
    = "#{t('activerecord.models.course.new_title')} #{@course&.name}"
  button.close[type="button" data-dismiss="modal" aria-label="Close"]
    span[aria-hidden="true"]
      | ×

= render 'form', student: @student
